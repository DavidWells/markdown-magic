const {
  getGlobGroupsFromArgs,
  isArrayLike,
  stringLooksLikeFile,
  getValue,
  trimLeadingDashes,
  removeNodeModules,
  coerceStringToRegex,
  convertToArray,
  addValue,
  customIsGlob,
} = require('dx-args')

module.exports = {
  getGlobGroupsFromArgs,
  isArrayLike,
  stringLooksLikeFile,
  getValue,
  trimLeadingDashes,
  removeNodeModules,
  coerceStringToRegex,
  convertToArray,
  addValue,
  customIsGlob,
}
